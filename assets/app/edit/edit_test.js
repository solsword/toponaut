'use strict';

describe('toponaut.edit module', function() {

  beforeEach(module('toponaut.edit'));

  describe('edit controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editCtrl = $controller('EditCtrl');
      expect(editCtrl).toBeDefined();
    }));

  });
});
