'use strict';

describe('toponaut.attract module', function() {

  beforeEach(module('toponaut.attract'));

  describe('attract controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var attractCtrl = $controller('AtractCtrl');
      expect(attractCtrl).toBeDefined();
    }));

  });
});
